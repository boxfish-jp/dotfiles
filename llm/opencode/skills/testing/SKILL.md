---
name: testing
description: テストを書く、テストを追加する、既存テストを見直す(レビュー・リファクタリング)時に必ずロードする。テスト名の文体(普通体/可能形)、group名付け、テストファイル構造と成功/失敗パスの網羅の規約を扱う。
---

# Testing Conventions

## Test Naming Conventions

テスト名は**仕様ドキュメント**として読めるように書く。テストランナーの出力がそのまま仕様一覧になることを意識する。

### 文体（普通体／可能形）
- **仕様の記述** → 普通体（終止形 `〜する`）
  - `getUserNameの値を返す`
  - `不正な入力でArgumentErrorを投げる`
- **使い方・ユースケース** → 可能形（`〜できる`）
  - `switch文でパターンマッチできる`
  - `バイト列から復元できる`

### group 名
テストの主題（クラス名やメソッド名）で始める。

```
group('Payload')                    # クラス名
group('fromJson')                   # メソッド名
group('全フィールドに値が必要')      # クラス名を親groupで示せている場合
```

### コード参照と日本語の間のスペース
変数名・メソッド名・キーワードなどのコード参照の直後には**スペースを入れない**。

```
getUserNameの値を返す          # OK
getUserName の値を返す          # NG
```

### テストファイルの構造
1テストファイル＝1クラス（または1機能単位）。パッケージ構成を test/ 以下にミラーする。

### テストの構造化
テスト対象メソッドごとにテストを分ける。同一メソッドの成功・失敗テストは同じグループにまとめる。

```
# OK: メソッドごとにまとめている
GetUserName
├── 成功時にユーザー名を返す
└── engine例外時にNATIVE_GET_USERNAMEエラーを返す

# NG: 成功と失敗が別々の場所にある
OnMethodCall
├── GetUserName（成功のみ）
└── ...
OnMethodCallFailure
├── GetUserName（失敗のみ）
└── ...
```

### 失敗パスも書く
成功パスだけでなく、例外・エラーパスもテストする。各メソッドのtry/catchパスを網羅的にカバーする。
